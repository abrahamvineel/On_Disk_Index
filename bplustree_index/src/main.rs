
struct Node {
    keys: Vec<i32>,
    children: Vec<usize>
}

struct LeafNode {
    keys: Vec<i32>,
    values: Vec<usize>
}

struct BPlusTree {
    root: Option<usize>
}

impl BPlusTree {

}

fn main() {

}
